{
  "enabled": {
    "description": "启用插件",
    "type": "bool",
    "default": true,
    "hint": "总开关，关闭后不会检测情绪和生成图片"
  },
  "gemini_api_settings": {
    "description": "生图 API 设置",
    "type": "object",
    "hint": "配置用于生成表情图片的 API（密钥和地址从提供商自动读取）",
    "items": {
      "provider_id": {
        "description": "生图模型提供商",
        "type": "string",
        "hint": "从 AstrBot 提供商列表中选择，自动读取模型/密钥/端点",
        "_special": "select_provider",
        "default": ""
      },
      "image_provider_type": {
        "description": "生图引擎",
        "type": "string",
        "hint": "gemini = Gemini API（图生图+文生图），grok = xAI Grok API（OpenAI 兼容格式）",
        "default": "gemini",
        "options": ["gemini", "grok"]
      },
      "model": {
        "description": "生图模型名称（可选覆盖）",
        "type": "string",
        "hint": "留空使用提供商默认模型。Gemini: gemini-2.0-flash-exp；Grok: grok-imagine-image",
        "default": ""
      },
      "timeout": {
        "description": "请求超时时间（秒）",
        "type": "int",
        "default": 60,
        "hint": "Gemini API 请求超时时间"
      },
      "resolution": {
        "description": "图片分辨率",
        "type": "string",
        "hint": "生成图片的分辨率",
        "default": "1K",
        "options": ["1K", "2K", "4K"]
      },
      "aspect_ratio": {
        "description": "图片长宽比",
        "type": "string",
        "hint": "生成图片的长宽比",
        "default": "1:1",
        "options": ["1:1", "16:9", "9:16", "4:3", "3:4", "3:2", "2:3"]
      }
    }
  },
  "mood_analysis_settings": {
    "description": "LLM 分析设置",
    "type": "object",
    "hint": "配置用于情绪分析、搜图分类、内容筛选的 LLM 提供商",
    "items": {
      "mood_provider_id": {
        "description": "LLM 提供商",
        "type": "string",
        "hint": "用于情绪分析、图片分类和筛选的 LLM，建议选择快速/便宜的模型（如 Gemini Flash）",
        "_special": "select_provider",
        "default": ""
      },
      "custom_mood_prompt": {
        "description": "自定义情绪分析提示词",
        "type": "string",
        "hint": "自定义分析提示词，留空使用默认。需包含 {categories} 占位符",
        "default": ""
      },
      "llm_timeout": {
        "description": "LLM 请求超时（秒）",
        "type": "int",
        "default": 30,
        "hint": "情绪分析、图片分类、内容筛选等 LLM 请求的超时时间（不含生图）"
      }
    }
  },
  "probability_settings": {
    "description": "触发概率设置",
    "type": "object",
    "hint": "LLM 分析回复语境后给出表达欲望评分（0-1），超过门槛则发图；发图后再以随机概率决定是否额外生图入库",
    "items": {
      "expression_threshold": {
        "description": "表达欲望门槛",
        "type": "float",
        "default": 0.65,
        "hint": "LLM 对每条回复评估表达欲望（0-1），评分 >= 此值才触发发图。0=每次都发，1=永不发。建议 0.3-0.8"
      },
      "llm_generation_enabled": {
        "description": "启用大模型生图",
        "type": "bool",
        "default": true,
        "hint": "是否启用大模型生成新表情图片。关闭后只从图库已有图片中随机抽取"
      },
      "llm_generation_probability": {
        "description": "大模型生图概率",
        "type": "int",
        "default": 30,
        "hint": "发图后，以此随机概率决定是否额外调用大模型生成新图入库（0-100）"
      }
    }
  },
  "generation_settings": {
    "description": "生图设置",
    "type": "object",
    "hint": "控制图片生成的参数",
    "items": {
      "image_prompt_template": {
        "description": "生图提示词模板",
        "type": "string",
        "hint": "{mood} 替换为心情",
        "default": "Generate a high-quality expressive emoji/sticker reaction image. The character should clearly convey the emotion: {mood}. Style: detailed, clean lines, vibrant colors, suitable for chat sticker. Square format, white or transparent background. Ensure high detail on facial features."
      },
      "reference_prompt_addon": {
        "description": "有参考图时的附加提示词",
        "type": "string",
        "hint": "当有参考图时追加的提示词",
        "default": "CRITICAL: Study ALL provided reference images carefully. The generated image MUST depict THE EXACT SAME CHARACTER with identical facial features, hairstyle, art style. Only change the expression to match the mood."
      },
      "sticker_mode": {
        "description": "表情包模式",
        "type": "bool",
        "default": false,
        "hint": "开启后将图片缩放为 200x200 正方形再发送，QQ 会以表情包样式展示（小图、无边框、点击不放大）"
      },
      "max_library_size": {
        "description": "图库总上限",
        "type": "int",
        "default": 0,
        "hint": "图库总图片数达到此值后，停止自动搜图和 LLM 生图。0=不限制"
      }
    }
  },
  "cooldown_settings": {
    "description": "冷却设置",
    "type": "object",
    "hint": "防止过于频繁地生成图片",
    "items": {
      "cooldown_seconds": {
        "description": "冷却时间（秒）",
        "type": "int",
        "default": 60,
        "hint": "两次生图之间的最短间隔"
      },
      "per_group": {
        "description": "按群组冷却",
        "type": "bool",
        "default": true,
        "hint": "true=每个群独立冷却，false=每个用户独立冷却"
      }
    }
  },
  "auto_update_settings": {
    "description": "自动图库更新",
    "type": "object",
    "hint": "定时从 Booru/Pixiv 搜索角色图片，LLM 自动分类心情后加入图库",
    "items": {
      "auto_update_enabled": {
        "description": "启用自动更新",
        "type": "bool",
        "default": false,
        "hint": "开启后按设定间隔自动搜图并分类入库"
      },
      "auto_update_interval_hours": {
        "description": "更新间隔（小时）",
        "type": "float",
        "default": 6,
        "hint": "每隔多少小时执行一次自动搜图，最小 0.5"
      },
      "auto_update_search_tags": {
        "description": "搜索标签",
        "type": "string",
        "default": "eris_greyrat solo",
        "hint": "Booru 搜索标签，空格分隔。注意不同站标签不同：danbooru 用 eris_greyrat，yande.re 用 eris_boreas_greyrat"
      },
      "auto_update_images_per_cycle": {
        "description": "每次搜索图片数",
        "type": "int",
        "default": 5,
        "hint": "每次自动更新搜索并下载的图片数量（1-50）"
      },
      "auto_update_source": {
        "description": "图片来源",
        "type": "string",
        "default": "danbooru",
        "options": ["danbooru", "yandere", "konachan", "pixiv"],
        "hint": "danbooru=danbooru.donmai.us（图最多），yandere=yande.re（高清），konachan=konachan.com，pixiv=pixiv.net（需要 refresh_token）"
      },
      "auto_update_min_score": {
        "description": "最低评分",
        "type": "int",
        "default": 10,
        "hint": "只下载评分高于此值的图片，过滤低质量内容（Pixiv 使用收藏数）"
      },
      "pixiv_refresh_token": {
        "description": "Pixiv Refresh Token",
        "type": "string",
        "default": "",
        "hint": "使用 Pixiv 图源时必填。通过 gppt login 命令获取"
      },
      "pixiv_search_keyword": {
        "description": "Pixiv 搜索关键词",
        "type": "string",
        "default": "",
        "hint": "（仅 Pixiv）完整的搜索词，空格分隔多个关键词表示同时包含。例如：エリス・ボレアス・グレイラット 無職転生。注意：填写后会完全替代上方的搜索标签，不会合并。留空则使用上方的搜索标签"
      },
      "pixiv_search_target": {
        "description": "Pixiv 搜索模式",
        "type": "string",
        "default": "partial_match_for_tags",
        "options": ["partial_match_for_tags", "exact_match_for_tags", "title_and_caption"],
        "hint": "partial_match_for_tags=标签部分匹配（推荐），exact_match_for_tags=标签精确匹配，title_and_caption=标题和简介含有关键词（结果杂）"
      },
      "pixiv_allow_r18": {
        "description": "允许 Pixiv R-18",
        "type": "bool",
        "default": false,
        "hint": "关闭时自动过滤 R-18/R-18G 作品，只搜全年龄内容"
      },
      "auto_update_filter_prompt": {
        "description": "搜图筛选提示词",
        "type": "string",
        "default": "",
        "hint": "搜图后由 LLM 判断是否入库的筛选条件。留空不筛选。例如：可以适度暴露，但不能不穿胸罩或内裤。满足条件输出 PASS，不满足输出 REJECT"
      }
    }
  }
}
